'use strict';
const fs=require('fs'),assert=require('assert');
const code=fs.readFileSync('review-prompt-v2.js','utf8'),sw=fs.readFileSync('service-worker.js','utf8'),html=fs.readFileSync('index.html','utf8');
assert.match(code,/THREE_DAYS=3\*86400000/);assert.match(code,/LATER=120\*86400000/);assert.match(code,/promptCount<2/);assert.match(code,/completedSessions>=3/);assert.match(code,/answered\(\)>=50/);assert.match(code,/solvedIds\.length>=100/);assert.match(code,/8\*3600000/);assert.match(sw,/review-prompt-v2\.js/);assert.ok(html.indexOf('store-config-v1.js')<html.indexOf('review-prompt-v2.js'));console.log('YKS review standard smoke OK');