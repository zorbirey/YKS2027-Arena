'use strict';
const fs=require('fs'),assert=require('assert');
const code=fs.readFileSync('engagement-v1.js','utf8'),tools=fs.readFileSync('yks-learning-tools-v1.js','utf8'),plans=fs.readFileSync('arena-plans-v1.js','utf8'),app=fs.readFileSync('app.js','utf8');
assert.match(code,/QUESTION_LIMIT=50/);assert.match(code,/REWARDED_AD_LIMIT=6/);assert.match(code,/verifiedByServer===true/);assert.match(code,/yks2027-achievement-v1/);assert.doesNotMatch(code,/cancel\.disabled=true/);assert.match(code,/yksarena:quiz-finished/);assert.match(tools,/90 soruluk doğrulanmış havuz/);assert.match(tools,/yks2027-wrongbook-v1/);assert.match(plans,/manualTransfer|DATA_KEYS/);assert.doesNotMatch(app,/serviceWorker\.register/);console.log('YKS engagement/core smoke OK');